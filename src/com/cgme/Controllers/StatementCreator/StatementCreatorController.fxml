<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<GridPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.cgme.Controllers.StatementCreator.StatementCreatorController"
            minHeight="400.0" minWidth="600.0" hgap="20" vgap="20">

    <!-- A display box for holding buttons -->
    <HBox GridPane.rowIndex="0" GridPane.columnIndex="0">
        <!-- When clicked, this closes the window -->
        <Button text="Cancel" fx:id="closeButton" onAction="#exitAction" />
    </HBox>

    <!-- Contains items for taking the "date" of the statement -->
    <HBox GridPane.rowIndex="1" GridPane.columnIndex="0">
        <Label text="Date: " />
        <!-- TODO: Finish implementing DatePicker -->
        <DatePicker fx:id="datePicker" />
    </HBox>

    <!-- Contains items for taking the "consolidated" of the statement -->
    <HBox GridPane.rowIndex="2" GridPane.columnIndex="0">
        <Label text="Is consolidated: " />
        <RadioButton fx:id="is_consolidated"/>
    </HBox>

    <!-- Contains items for taking the "price" of the statement -->
    <HBox GridPane.rowIndex="3" GridPane.columnIndex="0">
        <Label text="Price: " />
        <TextField editable="false" fx:id="price" />
        <Button text="Add pay period" onAction="#addPayPeriodAction" />
    </HBox>

    <HBox GridPane.rowIndex="4" GridPane.columnIndex="0">
        <Button text="Add New Statement" fx:id="addStatementButton" onAction="#addNewStatementAction" />
        <Button text="Finished" fx:id="finishedButton" onAction="#finishUpAction" />
    </HBox>

</GridPane>
